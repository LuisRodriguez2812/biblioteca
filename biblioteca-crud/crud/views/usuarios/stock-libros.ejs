<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Catálogo de Libros</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <%- include('../partials/header') %>
  <div class="container my-5">
    <h1>Catálogo de Libros</h1>
    <% if (libros.length > 0) { %>
      <% libros.forEach(libro => { %>
        <div class="card mb-3">
          <div class="card-body">
            <h5 class="card-title"><%= libro.titulo %></h5>
            <p class="card-text"><%= libro.autor %></p>
            <p class="card-text">Disponibles: <%= libro.disponibles %></p>
            <div class="d-flex justify-content-end">
              <a href="/reservas/nueva?libroId=<%= libro.id %>" class="btn btn-outline-primary me-2">Reservar</a>
              <a href="/prestamos/nuevo?libroId=<%= libro.id %>" class="btn btn-outline-success me-2">Prestar</a>
            </div>
          </div>          
        </div>
      <% }) %>
    <% } else { %>
      <p>No hay libros disponibles en este momento.</p>
    <% } %>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
